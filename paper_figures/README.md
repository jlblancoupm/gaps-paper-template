# Exact paper figures

`paper_figures/scripts/` must recreate the exact figures used in the paper.

`reference/` contains canonical publication assets.
`generated/` contains regenerated outputs for comparison.

Do not substitute visually similar recreations.
